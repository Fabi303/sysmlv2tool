This directory is created and used by the pilot implementation build, specifically during
building of the       

<groupId>com.sensmetry</groupId>
<artifactId>sysand-maven-plugin</artifactId>

the build requires at least a workspace file, so we put one here.